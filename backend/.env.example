# Variables de entorno para el backend
# Copiar este archivo a .env y ajustar los valores según tu entorno

# Puerto del servidor (opcional, default: 5000)
PORT=5000

# Entorno de ejecución (development | production)
NODE_ENV=development

# ===== MONGODB - ELIGE UNA OPCIÓN =====

# OPCIÓN 1: MongoDB Local (recomendado para desarrollo)
MONGODB_URI=mongodb://localhost:27017/sistema-compras

# OPCIÓN 2: MongoDB Atlas (nube - comenta la línea de arriba y descomenta esta)
# MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/sistema-compras?retryWrites=true&w=majority

# ========================================

# Secreto para firmar tokens JWT (usar un string aleatorio largo)
# IMPORTANTE: Cambia esto en producción
JWT_SECRET=tu_secreto_jwt_super_seguro_aqui

# Tiempo de expiración del token JWT (opcional, default: 7d)
JWT_EXPIRE=7d
